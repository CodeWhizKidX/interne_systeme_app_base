# .env ファイル

# ポート番号
PORT=3000

# Google OAuth 2.0設定
# Google Cloud Console > APIs & Services > Credentials で取得したClient IDを設定
# 注意: Client IDは公開情報ですが、Google Cloud Consoleで「承認済みのJavaScript生成元」を
# 厳密に設定することで、許可されていないドメインからの使用を防げます
GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"

# 許可された組織ドメイン（カンマ区切り）
# 例: ALLOWED_ORGANIZATION_DOMAINS="example.com,subdomain.example.com"
# すべてのドメインを許可する場合は "*" を設定（非推奨・セキュリティリスクあり）
# 重要: この設定により、組織外のユーザーはアクセスできません
ALLOWED_ORGANIZATION_DOMAINS="example.com"

# db関連
## DBユーザー
DB_USER="XXXXX"
## Cloud SQLのIPアドレス
DB_HOST="XXXXX"
## DB名
DB_NAME="XXXXX"
## パスワード
DB_PASS="XXXXX"

DATABASE_URL="XXXXX"

# .env ファイル

# ポート番号
PORT=3000

# Firebase Admin SDK設定
# 以下のいずれかの方法で設定してください：

# 方法1: サービスアカウントJSONを直接設定（本番環境推奨）
# JSON文字列をエスケープして1行で設定
# FIREBASE_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"your-project-id",...}'

# 方法2: サービスアカウントファイルのパスを指定（開発環境用）
# FIREBASE_SERVICE_ACCOUNT_PATH="./path/to/serviceAccountKey.json"

# 方法3: Google Cloud環境（Cloud Run, App Engineなど）では自動で認証情報を取得
# 環境変数の設定は不要

# db関連
## DBユーザー
DB_USER="XXXXX"
## Cloud SQLのIPアドレス
DB_HOST="XXXXX"
## DB名
DB_NAME="XXXXX"
## パスワード
DB_PASS="XXXXX"

DATABASE_URL="XXXXX"
